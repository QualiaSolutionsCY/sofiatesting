interface:
  display_name: "Sofiatesting"
  short_description: "Repo-specific patterns and workflows for sofiatesting"
  default_prompt: "Use the sofiatesting repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true