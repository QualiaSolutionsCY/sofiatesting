---
phase: 05-whatsapp-image-upload
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/functions/sophia-bot/index.ts
autonomous: false

must_haves:
  truths:
    - "Sophia-bot deploys successfully with image persistence"
    - "Phone gallery images are persisted and usable for Zyprus uploads"
    - "Error messages are helpful when image persistence fails"
  artifacts:
    - path: "supabase/functions/sophia-bot/index.ts"
      provides: "Deployed edge function with image persistence"
  key_links:
    - from: "sophia-bot edge function"
      to: "Supabase Storage"
      via: "persistImages service"
---

<objective>
Deploy and test WhatsApp image upload functionality

Purpose: Verify the image persistence flow works end-to-end in production

Output: Working image uploads from phone gallery via WhatsApp to Zyprus
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-whatsapp-image-upload/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy sophia-bot with image persistence</name>
  <files>supabase/functions/sophia-bot/index.ts</files>
  <action>
Deploy the updated sophia-bot edge function:

```bash
cd /home/qualia/Desktop/Projects/aiagents/sofiatesting
supabase functions deploy sophia-bot --no-verify-jwt --project-ref vceeheaxcrhmpqueudqx
```

Verify deployment succeeded (should show "Deployed" status).

If deployment fails, check for:
1. Syntax errors in image-persistence.ts
2. Import path issues
3. Missing env vars (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY are auto-set)
  </action>
  <verify>Deployment command shows success status</verify>
  <done>sophia-bot edge function deployed with image persistence</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
WhatsApp image persistence pipeline:
- Decrypted images from WaSenderAPI are re-uploaded to Supabase Storage
- Stable URLs are used for Zyprus property uploads
  </what-built>
  <how-to-verify>
1. Send a test image from your phone gallery to SOPHIA on WhatsApp
2. After SOPHIA responds, check Supabase Storage for the persisted image:
   - Go to https://supabase.com/dashboard/project/vceeheaxcrhmpqueudqx/storage/buckets
   - Open "documents" bucket
   - Look for "whatsapp-images/" folder
   - Verify image files exist with format: `wa_img_{timestamp}_{index}.jpg`

3. Check Edge Function logs for persistence messages:
   - Go to https://supabase.com/dashboard/project/vceeheaxcrhmpqueudqx/logs
   - Filter by "sophia-bot"
   - Look for: `[ImagePersist] Persisted image X to:`

4. (Optional) Create a property listing with phone gallery images:
   - Send 2-3 photos from your gallery
   - Tell SOPHIA details for a test listing
   - Verify images appear on the draft listing at dev9.zyprus.com/draft-dashboard
  </how-to-verify>
  <resume-signal>Type "approved" if images persist correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Edge function deployment:
   ```bash
   supabase functions list --project-ref vceeheaxcrhmpqueudqx | grep sophia-bot
   ```
   Should show "Active" status.

2. Manual WhatsApp test:
   - Send image from phone gallery
   - Check Supabase Storage for persisted file
   - Check Edge Function logs for success messages
</verification>

<success_criteria>
- sophia-bot deployed successfully
- Phone gallery images are persisted to Supabase Storage
- Images have stable URLs (not WaSenderAPI temporary URLs)
- Property listings can use phone gallery images
</success_criteria>

<output>
After completion, create `.planning/phases/05-whatsapp-image-upload/05-02-SUMMARY.md`
</output>
