---
phase: 23-type-safety-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sophia-bot/types/openrouter.ts
  - supabase/functions/sophia-bot/services/ai-chat.ts
  - supabase/functions/sophia-bot/zyprus/client.ts
autonomous: true

must_haves:
  truths:
    - "OpenRouter request/response bodies are strongly typed (no runtime errors from API schema changes)"
    - "Tool calls and tool definitions use TypeScript interfaces instead of any[]"
    - "TypeScript strict mode passes with zero any types in ai-chat.ts and zyprus/client.ts"
  artifacts:
    - path: "supabase/functions/sophia-bot/types/openrouter.ts"
      provides: "OpenRouter API TypeScript interfaces"
      exports: ["OpenRouterMessage", "OpenRouterToolCall", "OpenRouterFunctionCall", "OpenRouterResponse", "OpenRouterChoice"]
    - path: "supabase/functions/sophia-bot/services/ai-chat.ts"
      provides: "Type-safe OpenRouter integration (no any types)"
      min_lines: 880
    - path: "supabase/functions/sophia-bot/zyprus/client.ts"
      provides: "Type-safe error handling (no any in error mapping)"
      min_lines: 1870
  key_links:
    - from: "supabase/functions/sophia-bot/services/ai-chat.ts"
      to: "supabase/functions/sophia-bot/types/openrouter.ts"
      via: "import OpenRouter types"
      pattern: "import.*from.*types/openrouter"
---

<objective>
Create TypeScript interfaces for OpenRouter API and eliminate remaining `any` types from ai-chat.ts and zyprus/client.ts

Purpose: Prevent runtime type errors from OpenRouter API changes and improve type safety in error handling
Output: Fully typed AI service and Zyprus client with zero `any` type annotations
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/functions/sophia-bot/services/ai-chat.ts
@supabase/functions/sophia-bot/zyprus/client.ts
@supabase/functions/sophia-bot/tools/definitions.ts
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenRouter TypeScript interfaces</name>
  <files>supabase/functions/sophia-bot/types/openrouter.ts</files>
  <action>
Create comprehensive TypeScript interfaces for OpenRouter API based on actual usage in ai-chat.ts:

**Core interfaces needed (derive from ai-chat.ts):**

1. **OpenRouterMessage** - Message in conversation history (line 281-288, 415-427)
   - role: "system" | "user" | "assistant" | "tool"
   - content: string
   - tool_calls?: OpenRouterToolCall[] (line 284, 418)
   - tool_call_id?: string (for tool result messages - line 285, 419, 666)
   - name?: string (optional message attribution)

2. **OpenRouterToolCall** - Function call from AI (line 571-576, 612)
   - id: string (unique identifier for this tool call - line 666)
   - type: "function"
   - function: OpenRouterFunctionCall

3. **OpenRouterFunctionCall** - Function call details
   - name: string (tool name - line 613, 614)
   - arguments: string (JSON string of tool arguments - line 614, 617)

4. **OpenRouterResponse** - API response body (line 336-337)
   - choices: OpenRouterChoice[]
   - usage?: { prompt_tokens: number; completion_tokens: number; total_tokens: number }
   - id?: string
   - model?: string

5. **OpenRouterChoice** - Single response choice (line 337)
   - message: OpenRouterMessage
   - finish_reason?: "stop" | "length" | "tool_calls" | "content_filter"
   - index: number

6. **OpenRouterError** - Error response structure (line 340-342)
   - error: { status: number; message?: string; code?: string }

7. **OpenRouterRequestBody** - Request payload structure (line 321-328)
   - model: string
   - messages: OpenRouterMessage[]
   - temperature?: number
   - max_tokens?: number
   - tools?: OpenRouterTool[]
   - tool_choice?: "auto" | "required" | "none"

8. **OpenRouterTool** - Tool definition (from tools/definitions.ts pattern)
   - type: "function"
   - function: {
       name: string
       description: string
       parameters: Record<string, unknown>
     }

**Export all interfaces.** Add JSDoc comments explaining OpenRouter function calling flow and tool_calls structure.
  </action>
  <verify>
File exists: `supabase/functions/sophia-bot/types/openrouter.ts`
Contains exports: OpenRouterMessage, OpenRouterToolCall, OpenRouterResponse, OpenRouterRequestBody
TypeScript compiles without errors: `cd supabase/functions/sophia-bot && deno check types/openrouter.ts`
  </verify>
  <done>OpenRouter API structure fully typed with interfaces matching actual request/response patterns</done>
</task>

<task type="auto">
  <name>Task 2: Replace any types in ai-chat.ts</name>
  <files>supabase/functions/sophia-bot/services/ai-chat.ts</files>
  <action>
Replace all `any` types in ai-chat.ts with OpenRouter types:

**Changes needed:**

1. **Line 1: Add import**
   ```typescript
   import type { OpenRouterMessage, OpenRouterToolCall, OpenRouterResponse, OpenRouterTool } from "../types/openrouter.ts";
   ```

2. **Line 281-288: Type callOpenRouter messages parameter**
   - Change: `messages: Array<{ role: string; content: string; tool_calls?: any[]; tool_call_id?: string }>`
   - To: `messages: OpenRouterMessage[]`

3. **Line 287: Type tools parameter**
   - Change: `tools: any[]`
   - To: `tools: OpenRouterTool[]`

4. **Line 290: Type return value**
   - Change: `Promise<{ message: any; error?: string }>`
   - To: `Promise<{ message: OpenRouterMessage | null; error?: string }>`

5. **Line 415-420: Type openrouterMessages variable**
   - Change: `const openrouterMessages: Array<{ role: string; content: string; tool_calls?: any[]; tool_call_id?: string }> = ...`
   - To: `const openrouterMessages: OpenRouterMessage[] = ...`

6. **Line 425: Type history map function**
   - Change: `msg.parts.map((p: any) => p.text || "").join("")`
   - To: `msg.parts.map((p: { text?: string }) => p.text || "").join("")`

**After changes:**
- Verify the OpenRouter API response parsing (line 336-337) now benefits from type checking
- Ensure tool_calls array operations (line 571-600) are type-safe
- Confirm message construction (line 605-668) matches OpenRouterMessage interface

Run `npx tsc --noEmit` to verify no new type errors introduced.
  </action>
  <verify>
No `any` type annotations remain in ai-chat.ts except in comment text
TypeScript compilation succeeds: `cd /home/qualia/Projects/aiagents/sofiatesting && npx tsc --noEmit`
Grep confirms no function signatures with `: any` or `any[]`: `grep -n ": any" supabase/functions/sophia-bot/services/ai-chat.ts | grep -v "// "` returns empty
  </verify>
  <done>ai-chat.ts uses OpenRouter types throughout with zero any annotations in OpenRouter integration code</done>
</task>

<task type="auto">
  <name>Task 3: Replace any types in zyprus/client.ts error handling</name>
  <files>supabase/functions/sophia-bot/zyprus/client.ts</files>
  <action>
Replace the two `any` types in Zyprus client error handling (lines 1165 and 1771):

**Changes needed:**

1. **Line 1165: Type error mapping in property creation**
   - Current: `.map((e: any) => e.detail || e.title || JSON.stringify(e))`
   - Change to: `.map((e: { detail?: string; title?: string }) => e.detail || e.title || JSON.stringify(e))`
   - Context: This maps Zyprus API error objects to user-friendly messages

2. **Line 1771: Type error mapping in land creation**
   - Current: `.map((e: any) => e.detail || e.title || JSON.stringify(e))`
   - Change to: `.map((e: { detail?: string; title?: string }) => e.detail || e.title || JSON.stringify(e))`
   - Context: Same pattern as above for land listings

**Rationale:** Zyprus API returns JSON:API error format with `detail` and `title` fields. These are the only fields we access, so we can type them explicitly instead of using `any`.

**DO NOT change:**
- Lines 704, 828, 1504, 1559, 1691: These contain the word "any" in comments/strings (e.g., "any region", "any alias"), not type annotations

Run `npx tsc --noEmit` to verify no new type errors.
  </action>
  <verify>
No `any` type annotations remain in zyprus/client.ts function signatures
TypeScript compilation succeeds: `cd /home/qualia/Projects/aiagents/sofiatesting && npx tsc --noEmit`
Grep confirms only comment/string occurrences: `grep -n "\bany\b" supabase/functions/sophia-bot/zyprus/client.ts` shows only lines 704, 828, 1504, 1559, 1691 (comments)
  </verify>
  <done>zyprus/client.ts error handling is type-safe with explicit error object typing instead of any</done>
</task>

</tasks>

<verification>
1. Run TypeScript compiler across all changed files: `cd /home/qualia/Projects/aiagents/sofiatesting && npx tsc --noEmit`
2. Verify no `any` types in TYPE-03 files:
   - `grep -n ": any" supabase/functions/sophia-bot/services/message-processor.ts | grep -v "// "`
   - `grep -n ": any" supabase/functions/sophia-bot/services/ai-chat.ts | grep -v "// "`
   - `grep -n ": any" supabase/functions/sophia-bot/zyprus/client.ts | grep -v "// "`
3. All greps should return empty (only comments/strings allowed)
</verification>

<success_criteria>
- OpenRouter request/response structure fully documented in TypeScript interfaces
- ai-chat.ts OpenRouter integration strongly typed (messages, tool calls, responses)
- Zyprus client error handling uses explicit error object typing
- TypeScript strict mode passes with zero any types in TYPE-03 target files
- No runtime behavior changes (types are compile-time only)
- Phase 23 complete: All three TYPE requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/23-type-safety-foundation/23-02-SUMMARY.md`
</output>
