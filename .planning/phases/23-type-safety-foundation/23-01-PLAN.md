---
phase: 23-type-safety-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sophia-bot/types/wasend.ts
  - supabase/functions/sophia-bot/services/message-processor.ts
autonomous: true

must_haves:
  truths:
    - "WaSend webhook payload parsing is type-safe (no runtime type errors from API changes)"
    - "message, key, imageMessage fields are strongly typed in extractMessage"
    - "TypeScript strict mode catches missing properties in WaSend payload handling"
  artifacts:
    - path: "supabase/functions/sophia-bot/types/wasend.ts"
      provides: "WaSend webhook TypeScript interfaces"
      exports: ["WaSendWebhookPayload", "WaSendMessage", "WaSendKey", "WaSendImageMessage", "WaSendDocumentMessage"]
    - path: "supabase/functions/sophia-bot/services/message-processor.ts"
      provides: "Type-safe message extraction (no `any` in extractMessage, processImageMessage, generateMessageKey)"
      min_lines: 650
  key_links:
    - from: "supabase/functions/sophia-bot/services/message-processor.ts"
      to: "supabase/functions/sophia-bot/types/wasend.ts"
      via: "import WaSend types"
      pattern: "import.*from.*types/wasend"
---

<objective>
Create TypeScript interfaces for WaSend webhook payloads and eliminate `any` types from message-processor.ts

Purpose: Prevent runtime type errors from WaSend API changes by catching missing/renamed fields at compile time
Output: Type-safe webhook payload handling with zero `any` types in message extraction logic
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/functions/sophia-bot/services/message-processor.ts
@supabase/functions/sophia-bot/utils/wasend.ts
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WaSend TypeScript interfaces</name>
  <files>supabase/functions/sophia-bot/types/wasend.ts</files>
  <action>
Create comprehensive TypeScript interfaces for WaSend webhook payloads based on actual usage in message-processor.ts:

**Core interfaces needed (derive from message-processor.ts lines 28-220):**

1. **WaSendWebhookPayload** - Top-level webhook structure
   - event: string (messages.received, messages.upsert, message, messages)
   - data: WaSendData

2. **WaSendData** - Wrapper for messages
   - messages: WaSendMessage | WaSendMessage[] (NOTE: can be single object OR array per line 69)
   - message?: WaSendMessage (fallback field)

3. **WaSendMessage** - Core message structure
   - key?: WaSendKey (message metadata)
   - messageBody?: string (unified field for text/captions - line 204)
   - message?: WaSendMessageContent (nested content structure)
   - fromMe?: boolean (line 160)
   - remoteJid?: string (fallback phone - line 192)
   - from?: string, to?: string, phone?: string (fallback fields - line 192)
   - id?: string, messageId?: string (for deduplication - line 569-573)
   - messageTimestamp?: number (for deduplication - line 583)
   - media?: string[] (test format - line 433)
   - decryptedMediaUrl?: string (pre-decrypted images - line 304)
   - mediaUrl?: string (some webhook formats - line 316)
   - hasMedia?: boolean, messageType?: string, type?: string (image indicators - line 126)
   - imageMessage?: WaSendImageMessage (some variations - line 287)
   - data?: { imageMessage?: WaSendImageMessage } (another variation - line 292)

4. **WaSendKey** - Message identification
   - id?: string (message ID - line 219)
   - cleanedSenderPn?: string (recommended for private chats - line 174)
   - cleanedParticipantPn?: string (for group chats - line 174)
   - remoteJid?: string (fallback, check for LID format - line 177)
   - fromMe?: boolean (outgoing check - line 160)

5. **WaSendMessageContent** - Nested message content
   - conversation?: string (raw text - line 205)
   - extendedTextMessage?: { text: string } (rich text - line 206)
   - imageMessage?: WaSendImageMessage (standard location - line 279)
   - documentMessage?: WaSendDocumentMessage (attachments - line 329, 368)
   - text?: string (fallback - line 207)

6. **WaSendImageMessage** - Encrypted WhatsApp images
   - url: string (encrypted URL - line 228)
   - mimetype?: string (image/jpeg - line 245)
   - mediaKey?: string (decryption key - line 239)
   - fileSha256?: string (integrity check - line 247)
   - fileLength?: number | string (size - line 248)
   - caption?: string (image caption - line 207)

7. **WaSendDocumentMessage** - Document attachments
   - url: string (encrypted/public URL - line 329)
   - mimetype?: string (application/pdf, image/*, etc. - line 330, 369)
   - fileName?: string (original filename - line 351, 374)
   - mediaKey?: string (decryption key - line 341)
   - fileSha256?: string (integrity check - line 349)
   - fileLength?: number | string (size - line 350)

**Export all interfaces.** Add JSDoc comments explaining WaSend payload variations (single vs array messages, LID format warnings, etc.).
  </action>
  <verify>
File exists: `supabase/functions/sophia-bot/types/wasend.ts`
Contains exports: WaSendWebhookPayload, WaSendMessage, WaSendKey, WaSendImageMessage, WaSendDocumentMessage
TypeScript compiles without errors: `cd supabase/functions/sophia-bot && deno check types/wasend.ts`
  </verify>
  <done>WaSend webhook payload structure fully typed with interfaces matching actual usage patterns</done>
</task>

<task type="auto">
  <name>Task 2: Replace any types in message-processor.ts</name>
  <files>supabase/functions/sophia-bot/services/message-processor.ts</files>
  <action>
Replace all `any` types in message-processor.ts with WaSend types from types/wasend.ts:

**Changes needed:**

1. **Line 1: Add import**
   ```typescript
   import type { WaSendWebhookPayload, WaSendMessage, WaSendImageMessage, WaSendDocumentMessage } from "../types/wasend.ts";
   ```

2. **Line 28: Type extractMessage parameter**
   - Change: `extractMessage(payload: any)`
   - To: `extractMessage(payload: WaSendWebhookPayload)`

3. **Line 29: Type return message field**
   - Change: `message: any;`
   - To: `message: WaSendMessage;` in return type

4. **Line 46: Type local message variable**
   - Change: `let message = null;`
   - To: `let message: WaSendMessage | null = null;`

5. **Line 226: Type processImageMessage parameter**
   - Change: `const processImageMessage = async (imgMsg: any, source: string)`
   - To: `const processImageMessage = async (imgMsg: WaSendImageMessage, source: string)`

6. **Line 560: Type generateMessageKey parameter**
   - Change: `export function generateMessageKey(message: any): string | null`
   - To: `export function generateMessageKey(message: WaSendMessage): string | null`

**DO NOT change:**
- Line 506: "any valid images" (this is text, not a type annotation)
- Line 705: "if any" (this is text in a comment)

After changes, run `npx tsc --noEmit` from project root to verify no new type errors introduced.
  </action>
  <verify>
No `any` type annotations remain in extractMessage, processImageMessage, or generateMessageKey functions
TypeScript compilation succeeds: `cd /home/qualia/Projects/aiagents/sofiatesting && npx tsc --noEmit`
Grep confirms no function signatures with `: any`: `grep -n "function.*: any" supabase/functions/sophia-bot/services/message-processor.ts` returns empty
  </verify>
  <done>message-processor.ts uses WaSend types throughout with zero `any` annotations in webhook handling code</done>
</task>

</tasks>

<verification>
1. Run TypeScript compiler: `cd /home/qualia/Projects/aiagents/sofiatesting && npx tsc --noEmit`
2. Check for any types: `grep -n "\bany\b" supabase/functions/sophia-bot/services/message-processor.ts` should only show text strings, not type annotations
3. Verify import exists: `grep "import.*wasend" supabase/functions/sophia-bot/services/message-processor.ts`
</verification>

<success_criteria>
- WaSend webhook payload structure fully documented in TypeScript interfaces
- extractMessage function strongly typed (payload parameter, return type)
- processImageMessage helper strongly typed
- generateMessageKey strongly typed
- TypeScript strict mode passes with zero `any` types in message-processor.ts webhook handling
- No runtime behavior changes (types are compile-time only)
</success_criteria>

<output>
After completion, create `.planning/phases/23-type-safety-foundation/23-01-SUMMARY.md`
</output>
