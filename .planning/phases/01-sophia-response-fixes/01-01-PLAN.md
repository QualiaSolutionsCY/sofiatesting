---
phase: 01-sophia-response-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sophia-bot/prompts.ts
autonomous: true

must_haves:
  truths:
    - "SOPHIA never mentions 'Template 11', 'Template 12', or any 'Template XX' pattern to users"
    - "Document references use friendly names like 'Reservation Agreement' not numbers"
    - "Help/list responses describe documents by purpose, not by template number"
  artifacts:
    - path: "supabase/functions/sophia-bot/prompts.ts"
      provides: "System prompt without template number mentions"
      contains: "createPropertyListing"
  key_links:
    - from: "prompts.ts"
      to: "SOPHIA responses"
      via: "System prompt controls AI behavior"
      pattern: "never mention.*template.*number"
---

<objective>
Remove all template number mentions from SOPHIA's prompts so users never see "Template 11", "Template 12", etc.

Purpose: Users should interact with SOPHIA using natural language about documents (e.g., "reservation agreement", "viewing form") without seeing internal numbering.
Output: Updated prompts.ts with all "Template XX" references converted to user-friendly document names.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sophia-response-fixes/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit instruction to never mention template numbers</name>
  <files>supabase/functions/sophia-bot/prompts.ts</files>
  <action>
    Add a clear instruction at the TOP of the system prompt (in the main instructions section) that says:

    ```
    **CRITICAL - TEMPLATE REFERENCES:**
    NEVER mention template numbers to users (e.g., "Template 11", "Template 12").
    Always refer to documents by their friendly names:
    - "Reservation Agreement" (not "Template 12")
    - "Standard Viewing Form" (not "Template 09")
    - "Marketing Agreement" (not "Template 15")
    - etc.
    Internal template numbers are for system use only - users should never see them.
    ```

    This instruction should be prominent so Claude follows it consistently.
  </action>
  <verify>
    Search prompts.ts for the new instruction: `grep -n "NEVER mention template numbers" supabase/functions/sophia-bot/prompts.ts`
  </verify>
  <done>System prompt contains explicit instruction about not mentioning template numbers</done>
</task>

<task type="auto">
  <name>Task 2: Convert user-visible template references to friendly names</name>
  <files>supabase/functions/sophia-bot/prompts.ts</files>
  <action>
    Find and update all user-visible "Template XX" references. The file has ~100+ references.

    **Strategy:** Only change references that would be shown to users. Keep internal routing logic intact.

    Key patterns to find and convert (examples):
    - `"registration marketing together" → Use Template 02` → `"registration marketing together" → Use Seller with Marketing Agreement`
    - `*Standard Viewing Form* (Template 09)` → `*Standard Viewing Form*`
    - `Template 15: Non-Exclusive Marketing Agreement` in section headers → `Non-Exclusive Marketing Agreement`

    **PRESERVE internal routing comments** like:
    - `// Template 09` - These are developer comments, not user-visible
    - Variable names or function names with template numbers

    **Focus on:**
    1. Section headers that say "Template XX: Name"
    2. Bullet lists that mention "(Template XX)"
    3. Instructions that say "Use Template XX"
    4. Any text that would appear in SOPHIA's responses

    The goal is that if SOPHIA ever references a document, it uses the friendly name only.
  </action>
  <verify>
    Count remaining user-visible "Template XX" patterns (should be zero or only in developer comments):
    ```bash
    grep -E "Template (0[1-9]|[1-4][0-9])" supabase/functions/sophia-bot/prompts.ts | grep -v "^//" | wc -l
    ```
    Expected: 0 or minimal count (only in code comments, not instruction text)
  </verify>
  <done>All user-visible template number references converted to friendly document names</done>
</task>

<task type="auto">
  <name>Task 3: Test deployment with prompt changes</name>
  <files>supabase/functions/sophia-bot/prompts.ts</files>
  <action>
    Deploy the updated sophia-bot function to verify the prompt changes work:

    ```bash
    cd /home/qualia/Desktop/Projects/aiagents/sofiatesting
    supabase functions deploy sophia-bot --no-verify-jwt --project-ref vceeheaxcrhmpqueudqx
    ```

    Verify deployment succeeds without errors.
  </action>
  <verify>
    Check deployment output for success message. Run:
    ```bash
    supabase functions list --project-ref vceeheaxcrhmpqueudqx | grep sophia-bot
    ```
    Should show the function as deployed.
  </verify>
  <done>sophia-bot Edge Function deployed successfully with prompt updates</done>
</task>

</tasks>

<verification>
1. Search prompts.ts - no user-visible "Template XX" patterns remain
2. System prompt contains explicit "NEVER mention template numbers" instruction
3. Edge Function deployment succeeds
4. (Manual) Send a WhatsApp message asking "what documents can you create?" - response should list documents by name without numbers
</verification>

<success_criteria>
- SOPHIA's prompts no longer contain user-visible template number references
- System prompt explicitly instructs Claude to never mention template numbers
- Edge Function deploys successfully
- When users ask about documents, SOPHIA uses friendly names only
</success_criteria>

<output>
After completion, create `.planning/phases/01-sophia-response-fixes/01-01-SUMMARY.md`
</output>
