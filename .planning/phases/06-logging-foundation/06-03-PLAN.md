---
phase: 06-logging-foundation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - supabase/functions/sophia-bot/tools/executor.ts
  - supabase/functions/sophia-bot/zyprus/client.ts
  - supabase/functions/sophia-bot/services/pending-images.ts
  - supabase/functions/sophia-bot/services/image-persistence.ts
autonomous: true

must_haves:
  truths:
    - "Tool execution logs include tool name and correlation ID"
    - "Zyprus API calls logged with correlation ID for debugging"
    - "Image persistence logs include correlation ID"
    - "All 4 files use structured logger instead of console.log"
  artifacts:
    - path: "supabase/functions/sophia-bot/tools/executor.ts"
      provides: "Tool executor with structured logging"
      contains: "logger.info"
    - path: "supabase/functions/sophia-bot/zyprus/client.ts"
      provides: "Zyprus client with structured logging"
      contains: "logger.info"
    - path: "supabase/functions/sophia-bot/services/pending-images.ts"
      provides: "Pending images service with structured logging"
      contains: "logger.info"
    - path: "supabase/functions/sophia-bot/services/image-persistence.ts"
      provides: "Image persistence with structured logging"
      contains: "logger.info"
  key_links:
    - from: "supabase/functions/sophia-bot/tools/executor.ts"
      to: "supabase/functions/sophia-bot/utils/logger.ts"
      via: "logger import"
      pattern: 'import.*logger.*from.*logger'
---

<objective>
Migrate console.log calls in high-traffic secondary files to structured logger.

Purpose: Complete logging migration for the tool execution path (executor.ts, zyprus/client.ts) and image handling path (pending-images.ts, image-persistence.ts).

Output:
- 4 files fully migrated to structured logger
- Consistent logging across tool execution and image upload flows
- Foundation complete for LIST-06 integration in Plan 04
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context
@.planning/phases/06-logging-foundation/06-01-SUMMARY.md

# Key files
@supabase/functions/sophia-bot/utils/logger.ts
@supabase/functions/sophia-bot/utils/context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate tools/executor.ts (39 console.log calls)</name>
  <files>supabase/functions/sophia-bot/tools/executor.ts</files>
  <action>
1. Add import at top:
```typescript
import { logger, LogCategory, categorizeError } from "../utils/logger.ts";
```

2. Replace console.log calls with structured logger:

| Old | New | Category |
|-----|-----|----------|
| `console.log("[ToolExecutor] Executing:", tool.name)` | `logger.info("Executing tool", { category: LogCategory.TOOL, tool: tool.name })` | TOOL |
| `console.log("[ToolExecutor] Property listing...")` | `logger.info("Processing property listing", { category: LogCategory.TOOL, ... })` | TOOL |
| `console.error("[ToolExecutor] Error:", error)` | `logger.error("Tool execution failed", error, { category: LogCategory.TOOL, tool: tool.name, errorCategory: categorizeError(error) })` | TOOL |

3. For Zyprus-related logs within executor, use LogCategory.ZYPRUS:
```typescript
logger.info("Creating draft listing", { category: LogCategory.ZYPRUS, ... });
```

4. For image-related logs within executor, use LogCategory.IMAGE:
```typescript
logger.info("Processing images", { category: LogCategory.IMAGE, imageCount: urls.length });
```

Keep the context fields (tool name, arguments summary) but structure them as context object properties.
  </action>
  <verify>
- `grep -c "console\\.log\\|console\\.error" supabase/functions/sophia-bot/tools/executor.ts` returns 0
- Logger import present
- All log statements have appropriate LogCategory
- `deno check supabase/functions/sophia-bot/tools/executor.ts` passes
  </verify>
  <done>
executor.ts migrated to structured logger (39 calls).
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate zyprus/client.ts (16 console.log calls)</name>
  <files>supabase/functions/sophia-bot/zyprus/client.ts</files>
  <action>
1. Add import at top:
```typescript
import { logger, LogCategory, ErrorCategory, categorizeError } from "../utils/logger.ts";
```

2. Replace console.log calls with structured logger:

All logs in this file should use LogCategory.ZYPRUS since it's the Zyprus API client.

| Old | New |
|-----|-----|
| `console.log("[Zyprus] Authenticating...")` | `logger.info("Authenticating with Zyprus API", { category: LogCategory.ZYPRUS })` |
| `console.log("[Zyprus] Creating listing:", data)` | `logger.info("Creating listing", { category: LogCategory.ZYPRUS, propertyType: data.property_type })` |
| `console.error("[Zyprus] API Error:", error)` | `logger.error("Zyprus API request failed", error, { category: LogCategory.ZYPRUS, errorCategory: ErrorCategory.EXTERNAL_API })` |

3. For sensitive data (API tokens), ensure they're not logged:
```typescript
// DON'T: logger.info("Token received", { token: accessToken })
// DO: logger.info("Token received successfully", { category: LogCategory.ZYPRUS })
```

4. Include useful debugging info without sensitive data:
- Endpoint being called
- HTTP method
- Response status code
- Duration of API calls
  </action>
  <verify>
- `grep -c "console\\.log\\|console\\.error" supabase/functions/sophia-bot/zyprus/client.ts` returns 0
- Logger import present
- All logs use LogCategory.ZYPRUS
- No API tokens or secrets in log statements
- `deno check supabase/functions/sophia-bot/zyprus/client.ts` passes
  </verify>
  <done>
client.ts migrated to structured logger (16 calls).
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate image services (pending-images.ts + image-persistence.ts)</name>
  <files>supabase/functions/sophia-bot/services/pending-images.ts, supabase/functions/sophia-bot/services/image-persistence.ts</files>
  <action>
**pending-images.ts (9 console.log calls):**

1. Add import:
```typescript
import { logger, LogCategory, categorizeError } from "../utils/logger.ts";
```

2. Replace logs:
| Old | New |
|-----|-----|
| `console.log("[PendingImages] Adding X images for phoneNumber")` | `logger.info("Adding pending images", { category: LogCategory.IMAGE, count: imageUrls.length })` |
| `console.log("[PendingImages] Found X accumulated images")` | `logger.info("Retrieved pending images", { category: LogCategory.IMAGE, count: urls.length })` |
| `console.error("[PendingImages] Error:", error)` | `logger.error("Pending images operation failed", error, { category: LogCategory.IMAGE, errorCategory: categorizeError(error) })` |

Note: Phone number is PII - don't include it in logs. The logger will redact it if it appears in other context fields.

**image-persistence.ts (6 console.log calls):**

1. Add import:
```typescript
import { logger, LogCategory, categorizeError } from "../utils/logger.ts";
```

2. Replace logs:
| Old | New |
|-----|-----|
| `console.log("[ImagePersist] Persisting X images...")` | `logger.info("Persisting images to storage", { category: LogCategory.IMAGE, count: urls.length })` |
| `console.log("[ImagePersist] Persisted image N to: url")` | `logger.debug("Image persisted", { category: LogCategory.IMAGE, index: index })` |
| `console.error("[ImagePersist] Error:", error)` | `logger.error("Image persistence failed", error, { category: LogCategory.IMAGE, index, errorCategory: categorizeError(error) })` |

The persisted URL can be logged at debug level (not sensitive, but verbose).
  </action>
  <verify>
- `grep -c "console\\.log\\|console\\.error" supabase/functions/sophia-bot/services/pending-images.ts` returns 0
- `grep -c "console\\.log\\|console\\.error" supabase/functions/sophia-bot/services/image-persistence.ts` returns 0
- Both files import logger
- All logs use LogCategory.IMAGE
- `deno check supabase/functions/sophia-bot/services/pending-images.ts` passes
- `deno check supabase/functions/sophia-bot/services/image-persistence.ts` passes
  </verify>
  <done>
Image services migrated to structured logger (15 calls total).
  </done>
</task>

</tasks>

<verification>
1. `deno check` passes for all 4 files
2. Total console.log calls migrated: 39 + 16 + 9 + 6 = 70
3. All files use appropriate LogCategory
4. Error logs include errorCategory
5. No sensitive data (tokens, phone numbers) explicitly logged
6. Existing functionality unchanged
</verification>

<success_criteria>
- executor.ts: 0 console.log calls, structured logging throughout
- client.ts: 0 console.log calls, structured logging throughout
- pending-images.ts: 0 console.log calls, structured logging throughout
- image-persistence.ts: 0 console.log calls, structured logging throughout
- All files pass deno check
- Logging categories consistent (TOOL, ZYPRUS, IMAGE)
</success_criteria>

<output>
After completion, create `.planning/phases/06-logging-foundation/06-03-SUMMARY.md`
</output>
