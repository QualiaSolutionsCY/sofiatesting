---
phase: 19-authentication-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/storage/upload-file.ts]
autonomous: true

must_haves:
  truths:
    - "Service role key cannot be imported in client-side code"
    - "Upload functionality still works from server components and API routes"
  artifacts:
    - path: "lib/storage/upload-file.ts"
      provides: "Server-only Supabase admin client with service role key"
      contains: "import 'server-only'"
      min_lines: 10
  key_links:
    - from: "lib/storage/upload-file.ts"
      to: "SUPABASE_SERVICE_ROLE_KEY"
      via: "process.env access with server-only guard"
      pattern: "import 'server-only'"
---

<objective>
Protect service role key from client-side exposure by adding server-only import guard.

Purpose: Prevent accidental import of service role key in client components, which would expose the key in browser bundles and allow unauthorized database access bypassing RLS.

Output: Service role key isolated to server-side code only with build-time enforcement.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-database-security-rls/18-01-SUMMARY.md
@lib/storage/upload-file.ts
</context>

<tasks>

<task type="auto">
  <name>Add server-only import to upload-file.ts</name>
  <files>lib/storage/upload-file.ts</files>
  <action>
Add `import 'server-only';` as the first import in lib/storage/upload-file.ts (before all other imports).

This is a Next.js build-time enforcement mechanism that prevents this module from being imported in client components. Since this file uses SUPABASE_SERVICE_ROLE_KEY, it must never be accessible from browser code.

Pattern (from Next.js docs):
```typescript
import 'server-only';

import { createClient } from '@supabase/supabase-js';
// ... rest of imports
```

The 'server-only' package is already installed in this project (check package.json if needed).
  </action>
  <verify>
1. Read lib/storage/upload-file.ts and confirm `import 'server-only';` is the first line
2. Run `pnpm build` and verify no build errors related to server-only imports
3. Search for any client component imports of this file: `grep -r "from.*storage/upload-file" app/` should only return server components (files without 'use client')
  </verify>
  <done>
- lib/storage/upload-file.ts has `import 'server-only';` as first import
- Build succeeds without errors
- No client components import this file (or build would fail with server-only violation)
  </done>
</task>

</tasks>

<verification>
**Build-time check:**
```bash
pnpm build
# Should succeed - no server-only violations
```

**Runtime check:**
Service role key remains functional for server-side operations (file upload API route works).

**Security check:**
If someone tries to import this file in a client component, they get a clear build error pointing to the server-only violation.
</verification>

<success_criteria>
1. `import 'server-only';` present in lib/storage/upload-file.ts
2. Production build succeeds
3. File upload functionality unaffected (server API routes still work)
4. Attempting to import in client component causes build failure
</success_criteria>

<output>
After completion, create `.planning/phases/19-authentication-hardening/19-01-SUMMARY.md`
</output>
