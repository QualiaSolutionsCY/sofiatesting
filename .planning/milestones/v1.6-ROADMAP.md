# Milestone v1.6: Upload Pipeline Production Hardening

**Status:** PLANNED
**Phases:** 26-27
**Estimated Plans:** 4-6

## Overview

Fix all 17 issues from the 2026-03-20 AI review of Sophia's upload pipeline. Phase 26 handles the data-integrity and lock issues (things that cause uploads to fail or produce wrong data). Phase 27 handles business-rule enforcement and cleanup (things that cause wrong assignments or redundant code).

## Phases

### Phase 26: Upload Data Integrity Fixes

**Goal:** Zero silent data loss, zero lock leaks, zero broken email follow-ups
**Depends on:** Nothing
**Scope:** FR-1, FR-2, FR-3, FR-4, FR-5, FR-6, FR-8

Files touched:
- `tools/handlers/field-validation.ts` — Lock release on all paths (FR-1)
- `rules/special-cases.ts` — Add listingType to required fields (FR-2)
- `tools/schemas.ts` — Remove bedrooms default, make listingType required (FR-2, FR-8)
- `services/ai-chat.ts` — Fix parsePreExtractedFields regex + poolType parsing + bedrooms nullableFields (FR-4, FR-5, FR-6)
- `handlers/email-webhook.ts` — Store Google Maps block response in chat_history + load history for follow-ups (FR-3)

**Why one phase:** These are all code-level bugs where a value is silently lost, corrupted, or a lock is leaked. They're independent fixes that don't interact with each other. Ship together, test together.

### Phase 27: Business Rules & Cleanup

**Goal:** All business rules enforced, no redundant code, no dropped fields
**Depends on:** Phase 26 (data must be correct before rules matter)
**Scope:** FR-7, FR-9, FR-10, FR-11, FR-12

Files touched:
- `tools/handlers/field-validation.ts` — Apply modifiedRequest.assignTo from special cases (FR-7)
- `services/ai-chat.ts` — Remove redundant clearPendingImages (FR-9)
- `tools/handlers/property-listing.ts` — Pass condition to createDraftListing (FR-10)
- `handlers/email-webhook.ts` — Add "assign this to" pattern (FR-11)
- `tools/handlers/field-validation.ts` or new shared types file — Deduplicate ToolResult (FR-12)

**Why one phase:** These are business-logic correctness and code quality. Lower risk, less interplay. Ship as one batch after the critical data-integrity fixes land.

### Post-Phase: Deploy & Verify

After both phases:
1. Deploy sophia-bot
2. WhatsApp test upload (property with all optional fields)
3. Email test upload (structured email with images)
4. Check logs for lock leak warnings
5. Test Michelle rental flow (verify Demetra assignment)

---

## Risk Assessment

**Low risk:** All changes are isolated to the upload pipeline. No prompt changes, no schema changes, no new tables. The fixes are surgical — each one touches a specific code path.

**Rollback:** If issues arise, redeploy from the pre-v1.6 commit. Upload pipeline is stateless (no migrations).
