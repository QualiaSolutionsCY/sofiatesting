# Fix Report - 2026-06-23 19:45

**Symptom:** The Monthly Run review screen ("N drafts ready for your review") showed each draft with the SOURCE invoice's current month ("June 2026"), even though clicking "Create 2" generates NEXT month's invoices. Marios: "the next month will say July when she creates it again; the only change will be the month."
**Mode:** quick
**Outcome:** fixed

## Feedback Loop
- Initial check: live browser — Monthly Run overlay rows read "Bill To · June 2026" while the created invoices are dated July.
- Final check: `npx tsc --noEmit` PASS + live browser — overlay rows now read "Bill To · July 2026", created invoices № 11434/11435 dated 23 July 2026.

## Root Cause
`components/invoices/redesign/App.tsx` (monthlyRows useMemo) — `period: d.period || ""` fed the overlay + preview the source invoice's current period; the run creates next month (onApproveAll already rolls description via rollDescriptionMonth + advances issueDate via addOneMonth), so the review screen contradicted the output.

## Patch
- Files changed: components/invoices/redesign/App.tsx (1 file)
- Change summary: `period: rollDescriptionMonth(d.period || "")` so the run's displayed period (overlay rows, PDF-batch preview, and create fallback description) reflects the NEXT month it generates. Reuses the existing format.ts helper; no new behavior.

## Verification
- `npx tsc --noEmit` - PASS
- Live: Monthly Run overlay shows "July 2026"; "Create 2" created 11434/11435 dated 23 July 2026 - PASS

## Remaining Notes
- Monthly run still creates DRAFTS (review-then-approve), not auto-sent. Unattended pg_cron auto-generation remains deferred (M8 follow-up). Not committed yet — held pending the user's full live-test sign-off before merge/deploy.
