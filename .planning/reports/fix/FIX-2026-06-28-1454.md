# Fix Report - 2026-06-28 14:54

**Symptom:** In the invoice "Client delivery · on Marios's behalf" card, the message box shows a composed default ("Hello jameel, on behalf of Marios at CSC Zyprus — your invoice 11460 for June 2026 is attached. Total €12,932. Due 2026-07-10. — via Sophia"), but the email the client actually receives uses a generic body ("Dear jameel mawahreh, Please find attached invoice 11460 from CSC Zyprus Property Group. Kind regards, CSC Zyprus Property Group"). The visible message is not what's sent.
**Mode:** quick (frontend)
**Outcome:** fixed

## Feedback Loop
- Initial: two user screenshots (panel box vs received email) + code trace of the "Send email" path.
- Final: `npx tsc --noEmit` clean for `App.tsx`.

## Root Cause
`components/invoices/redesign/App.tsx:567` (`case "client-send-all"`) called `sendInvoiceEmailAction(selected.id, recipients, clientMsgOverrides[selected.id] || undefined)`. The action (`lib/invoices/actions/documents.ts:456-461`) DOES honor a `customMessage` when passed, but falls back to a generic template ("Dear … Please find attached … from CSC Zyprus Property Group … Kind regards, CSC Zyprus Property Group") when it's empty. The composed default message is built **only for display** in `components/invoices/redesign/ledger/DetailPane.tsx:626-634` and was never threaded into the send — so unless the operator explicitly clicked "Edit message" and saved an override (`clientMsgOverrides`), the send passed `undefined` and the client got the generic body instead of the message shown on screen.

## Patch
- Files changed: `components/invoices/redesign/App.tsx` (1 file)
- Change summary: In the `client-send-all` handler, build the same composed client-delivery message the card displays (mirror of `DetailPane.tsx:626-634` — resolves the client via `clientById(selected.client).name`, uses `selected.officialNo/period/total/due/kind`, formats with `fmt`) and pass it as the fallback: `clientMsgOverrides[selected.id] || composedClientMessage`. Added `fmt` to the existing `@/lib/invoices/redesign/data` import. The saved "Edit message" override still wins; only the previously-dropped default now actually sends.

## Verification
- `npx tsc --noEmit` (App.tsx) — PASS (no type errors; first attempt caught a wrong field `selected.clientName` → corrected to `clientById(selected.client).name`, the same resolution DetailPane uses)
- Manual reasoning: no override → client receives the exact "Hello … on behalf of Marios … — via Sophia" text shown in the box ✓; with an "Edit message" override saved → override still sent ✓; recurring CC to marios@zyprus.com unchanged ✓

## Remaining Notes
- **Template is now duplicated** between `DetailPane.tsx:626-634` (display) and `App.tsx` (send). They must stay in sync. Durable follow-up (out of `--quick` 1-file scope): extract a single `buildClientDeliveryMessage(doc, client)` helper in `lib/invoices/redesign/data.ts` and use it in both — a 2-file refactor.
- The Sophia WhatsApp `email_invoice` path (`lib/invoices/sophia/intent-handlers.ts:461`) calls `sendInvoiceEmailAction(doc.id, recipients)` with no message → it also uses the generic body. Out of scope here; flag if Marios wants the composed message on that path too.
