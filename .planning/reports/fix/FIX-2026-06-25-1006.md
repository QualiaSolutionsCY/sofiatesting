# Fix Report - 2026-06-25 10:06

**Symptom:** Marios's WhatsApp screenshot of a credit-note message from Sophia — he circled the trailing paragraph "Please read it alongside the attached credit note." and said "I dont need this extra paragraph bro for the credit notes."
**Mode:** quick
**Outcome:** fixed

## Feedback Loop
- Initial inspection: `grep -rn "read it alongside"` → single source.
- Final passing check: `npx tsc --noEmit` (0) + `npx tsx --test lib/invoices/lifecycle.test.ts` (33/33).

## Root Cause
`lib/invoices/actions/documents.ts:463-465` — `notifyGroupOfCreditNote()` built the accounting-group caption as two paragraphs: the cancellation line + `\n\n` + "Please read it alongside the attached credit note." The second paragraph is the unwanted "extra info".

## Patch
- Files changed: `lib/invoices/actions/documents.ts` (1 file)
- Change summary: dropped the `\n\n` + "Please read it alongside the attached credit note." tail. The credit-note group caption is now a single line: `Credit note {№} regarding the cancellation of invoice {№}.{ reason}`.

## Verification
- `npx tsc --noEmit` — PASS (exit 0)
- `npx tsx --test lib/invoices/lifecycle.test.ts` — PASS (33/33)
- New caption example: "Credit note 10104 regarding the cancellation of invoice 11444. The reason is: …" — no trailing paragraph.

## Remaining Notes
- Only `notifyGroupOfCreditNote` produced that string (confirmed by grep). Marios's own credit-note copy (`notifyMariosOverWhatsApp`) and `buildWhatsappCaption` use different text and were not touched.
- Not deployed yet — change is committed locally on `feature/moayad-invoicing-access`.
