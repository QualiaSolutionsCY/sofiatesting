# Fix Report - 2026-06-26 15:47

**Symptom:** Marios's 4 invoicing requests — (1) commission recognition incl. "rent of", (2) edited email/message must show in the client delivery box, (3) always CC marios@zyprus.com on monthly/yearly, (4) Bill-To name on the invoice without other details.
**Mode:** multi-file (NOT --quick — 5 files across 4 fixes)
**Outcome:** 3 of 4 fixed; #4 awaiting clarification (already appears done)

## Root Causes & Patches

### Fix 1 — commission recognition (commission / sale of / rent of)
- `lib/invoices/format.ts:65` — `isCommissionDescription` matched `commission` / `property sale` / `sale of property|land|plot` but NOT `rent of`. Broadened to `commission | property sale | sale of | rent of | letting of`. Plain "Monthly rent for June" stays non-commission (no "rent of").
- `supabase/functions/sophia-bot/prompts/behaviors/invoicing.ts:30,39,43` — added "rent of …" / "letting of …" to the commission triggers and made the mandatory question "Which agent (or agents) made the sale **or rental**?" (`invoicing` is in FILE_OVERRIDE_KEYS, so the file is the source of truth — needs edge-fn deploy).
- Tests: `tests/unit/invoice-marios-scenarios.test.ts` — added rent-of/letting-of cases (16/16 pass).

### Fix 2 — edited email/message reflects in the client delivery box
- `components/invoices/redesign/ledger/DetailPane.tsx` — the edited *email* already reflected (clientEmail prop), but the edited *message* (`clientMsgOverrides`) was never passed to the card; the card msg was a hardcoded template (`DetailPane.tsx:619`). Threaded a new `clientMsg` prop (DetailPaneProps → DetailPane → DeliveryPlan) and the client card now shows the override verbatim when set, else the default template.
- `components/invoices/redesign/App.tsx` — passes `clientMsg={clientMsgOverrides[selected.id]}` to DetailPane.

### Fix 3 — always CC marios@zyprus.com on monthly/yearly (+ bill-to)
- `lib/invoices/sophia/intent-handlers.ts:138` — the CC relied on `process.env.INVOICE_MARIOS_EMAIL`, which is NOT set in Vercel, so Marios was silently never CC'd. Defaulted to `"marios@zyprus.com"`.
- `components/invoices/redesign/App.tsx` (`client-send-all`) — the admin "Send email" added no CC at all. Now, for recurring (monthly/yearly) invoices, recipients = `[bill-to, marios@zyprus.com]`; one-off invoices unchanged. Toast notes the CC.

### Fix 4 — Bill-To name on the invoice, no other details
- INVESTIGATED, appears ALREADY satisfied: `lib/invoices/pdf.ts:148-152` renders `document.clientName` with an explicit "No client email under the name" comment; `TemplatePreview.tsx:87-90` shows name + VAT only. No email/extra detail is rendered. Awaiting Marios's screenshot to see the specific defect (missing name on a batch/recurring invoice? a blank clientName? a label wanted?).

## Verification
- `npx tsc --noEmit` — PASS (exit 0)
- `npx tsx --test tests/unit/invoice-marios-scenarios.test.ts lib/invoices/lifecycle.test.ts` — PASS (was 48/48; +2 new rent-of cases)
- Live admin/Sophia behavior — PENDING deploy (Vercel for UI/email + sophia-bot edge fn for the prompt).

## Remaining Notes
- Not deployed yet — committed on `fix/marios-invoice-4fixes`. Deploy needs BOTH Vercel (`vercel --prod`) and the sophia-bot edge fn (for Fix 1 prompt).
- Fix 3 also worth setting `INVOICE_MARIOS_EMAIL=marios@zyprus.com` in Vercel as belt-and-braces (the code now defaults regardless).
