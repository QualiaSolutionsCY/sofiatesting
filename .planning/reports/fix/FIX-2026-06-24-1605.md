# Fix Report - 2026-06-24 16:05

**Symptom:** (1) The "Actions" dropdown stays open when switching to another invoice/tab — should auto-close. (2) "Where is the Save button?" — editing a document should save immediately.
**Mode:** quick (1 file)
**Outcome:** fixed

## Feedback Loop
- Initial: image shows the Actions <details> menu stuck open, overlapping the editor toolbar (where the Save button lives) while another doc is selected.
- Final: `npx tsc --noEmit` PASS; menu closes on doc switch + outside-click, revealing the toolbar.

## Root Cause
`components/invoices/redesign/ledger/DetailPane.tsx:697` — `moreOpen` (Actions menu) was never reset on `doc` change, and native `<details>` doesn't close on outside-click, so the menu persisted across invoice switches and **covered the inline-editor toolbar** that holds the Save button (`DetailPane.tsx:234-247`). The Save button itself was already wired + immediate (`handleSave`→`onUpdate`/`onCorrectResend`, line 171-180); it was hidden, not missing.

## Patch
- Files changed: `components/invoices/redesign/ledger/DetailPane.tsx` (1)
- Added `useRef` import; a `moreRef` + two effects: reset `moreOpen` on `doc?.id` change, and close on outside `mousedown`; added `ref={moreRef}` to the `<details>`.

## Verification
- `npx tsc --noEmit` — PASS
- Live click-through after deploy — PENDING (user)

## Remaining Notes
- Save button is at the top of the edit panel ("Save changes" for drafts = immediate; "Correct & resend" for already-sent invoices, by Marios's design). Now visible once the menu closes. If a bottom/sticky Save is wanted, that's a follow-up enhancement (out of --quick scope).
