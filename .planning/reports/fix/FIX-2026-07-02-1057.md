# Fix Report - 2026-07-02 10:57

**Symptom:** Make the default invoice email match Marios Polyviou's exact signature (text/spacing/structure + Zyprus logo) across every send path (WhatsApp monthly/yearly/one-off, panel, client-delivery). Also fix the invoice PDF template to match Invoice 11491 but without the "OVERDUE" stamp.
**Mode:** quick
**Outcome:** fixed

## Feedback Loop
- Initial failing check: inspection — `lib/invoices/email.ts` signed the body as "Sophia" with the old letterhead; the request is Marios Polyviou's signature. PDF: reference 11491 shows "OVERDUE".
- Final passing check: `npx tsc --noEmit` (exit 0) + `tsx --test lib/invoices/*.test.ts tests/unit/invoice-marios-scenarios.test.ts` (49/49) + rendered text+HTML output verified to match the pasted signature.

## Root Cause
`lib/invoices/email.ts:63-88` (body) + `:97-109` (HTML twin) — the single source of the default invoice email (used by WhatsApp via invoice-bridge → /api/sophia/intent, the panel, and client-delivery) signed as "Sophia" and rendered a flat pre-wrap block, not Marios's structured letterhead.

## Patch
- Files changed: `lib/invoices/email.ts` (one file — covers all paths; `clientDeliveryMessage` reuses `buildInvoiceEmailBody`).
- Change summary:
  1. `buildInvoiceEmailBody`: kept the greeting + "on behalf of" + "Bill due/Date Due" + the "Sent by Sophia, CSC Zyprus's AI assistant." / "Please don't reply" disclosure (user chose "keep AI disclosure"), then Marios Polyviou's exact signature with matching double-spaced structure. Removed the redundant "For any correspondence…" line (Marios is now the signatory).
  2. `invoiceEmailBodyToHtml`: line-by-line HTML — bold brand lines (Real Estate Consultant, Zyprus Property Group, Licensed…, CSC…LTD), bold T:/M:/W:/E: labels, clickable www.zyprus.com + @zyprus.com links, blank-line spacers, and the Zyprus logo (public/assets/zyprus-logo.png) as a footer.
- PDF template: NO change required — `lib/invoices/pdf.ts` already renders "no status stamp" (line 111) and mirrors Invoice 11491's layout. The "OVERDUE" in the reference PDF is from the old Zoho export; our generator never stamps it (grep for "overdue" in lib/app/components = 0 hits).

## Verification
- `npx tsc --noEmit` - PASS (exit 0)
- `tsx --test lib/invoices/*.test.ts tests/unit/invoice-marios-scenarios.test.ts` - PASS (49/49)
- Rendered sample email text + HTML - PASS (structure + logo match)

## Remaining Notes
- Remote images (the logo) are blocked-by-default in some email clients until the recipient enables images — the text signature always shows regardless.
- Deploying to Vercel covers the WhatsApp path too (its invoice-bridge targets the same Next.js app at /api/sophia/intent).
