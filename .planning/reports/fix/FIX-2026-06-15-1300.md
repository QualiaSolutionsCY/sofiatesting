# Fix Report - 2026-06-15 13:00

**Symptom:** Bank-portal hardening (Lauren's remaining notes) — Altamira missing veranda/energy/land-size; "said NO POOL but the bank description mentions a pool"; photo dedup/order.
**Mode:** general (extraction quality)
**Outcome:** partial — verified wins shipped & LIVE; JS-rendered-only fields honestly deferred.

## Approach
Evidence-based against the live Altamira page (`pr43514`), each regex tested against the fetched HTML before shipping.

## Shipped (services/portal-scraper.ts, Altamira)
- **Core specs from authoritative hidden inputs** — `metros`→coveredArea (482), `dormitorios`→bedrooms (6), `nbanos`→bathrooms (6). Same class of fix as the price (`precio`) one — these server-side hidden inputs are reliable even when the JS-rendered specs row is missed.
- **Pool from description (Lauren #6)** — adds a pool ONLY on an explicit, non-negated "swimming pool" mention in the page text (this listing: "overflow swimming pool"). Never invents one; `groundFeatures` keeps it because the word is on the page.
- **Covered veranda from prose** — catches "covered veranda … 82 sq.m" (82 verified).

All five values confirmed by simulating the new parse on the live HTML: `{coveredArea:482, bedrooms:6, bathrooms:6, features:["swimming pool"], coveredVeranda:82}`.

## Honestly deferred (NOT fixable from raw HTML)
- **Land/plot size (739 m²)** — not present in the raw HTML at all; the specs row is JS-rendered. Needs the Firecrawl-rendered markdown path (and testing across listings), not a raw-HTML regex.
- **Energy class** — rendered as a graphic (`alt="energy rating"`), no letter in the raw HTML.
- **Photo dedup/order + bank-agent avatar** — the structured Altamira gallery already dedups (a `Set` of `…/grandes/…jpg`) and excludes the agent path; the "multiple copies / avatar" reports were from the older Firecrawl-LLM path. Genuine ordering (which photo is the cover) is ambiguous and risky to change blind — left for the increment with multi-listing testing.

## Verification
- `supabase functions deploy sophia-bot` — PASS (clean bundle = compiles).
- Live-HTML simulation of the new logic — PASS (values above).
- Behavioural (pending): re-send the Altamira `pr43514` link — should now show covered area, 6/6, veranda 82 m², and a pool; price already fixed. (Land size + energy class will still be absent — that's the deferred JS-rendered work.)
