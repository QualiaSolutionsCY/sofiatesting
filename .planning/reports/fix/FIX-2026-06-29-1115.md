# Fix Report - 2026-06-29 11:15

**Symptom:** Lauren's all-banks QA (2026-06-29): ALTIA dup/missing photos; REMU no property photos (grabbed a wrong image), no Google location link, pin in wrong place; Altamira invented a pool ("communal pool" in description + "private pool" in features) though there is no pool; SOPHIA asking the agent for price on bank links. Gogordian OK.
**Mode:** general
**Outcome:** partial (3 high-confidence root causes fixed; 2 deferred — need live-page inspection)

## Feedback Loop
- Initial inspection: `git show 0148e62` proved the REMU gallery + Altamira price fixes existed and were live-verified 2026-06-15, then lost when `recover/sophia-latest` reset `portal-scraper.ts` to an older state (`git log 0148e62..HEAD -- portal-scraper.ts` shows the restore never re-landed; current code still has the broken `id="precio"` regex and no `fetchRemuGallery`).
- Final check: node fixtures exercised the 4 new code paths — pool phrase detection, description pool-scrub, Altamira `name="precio"` match (→ 850000), REMU listing-id → handler-URL derivation (→ ref 28513). **ALL LOGIC CHECKS PASS.** Bundle/type check via `supabase functions deploy sophia-bot`.

## Root Cause
- `portal-scraper.ts` (REMU) — `fetchRemuGallery()` and its call site were dropped in the recovery reset; REMU's gallery is served by extension-less `imgHandler.ashx?ref=ID&order=N&serial=1`, so the generic `<img>`/markdown/Firecrawl extractors grab a placeholder/wrong image → "no property photos, picture of something else."
- `portal-scraper.ts:~1016` (Altamira price) — current regex matches `id="precio"`, but the real element is `name="precio" value="…" id="formOportunidad_precio"`; price never extracts → SOPHIA asks the agent for it.
- `portal-scraper.ts` `groundFeatures()` — kept a feature when ANY ≥4-char word matched, so an invented "private pool" survived on the bare word "pool"; the description was never grounded at all → phantom pool in both fields.

## Patch
- Files changed: `supabase/functions/sophia-bot/services/portal-scraper.ts` (1 file)
- Change summary:
  1. Restored `fetchRemuGallery()` + Phase-4 call site (verbatim from `0148e62`, live-verified) — authoritative REMU gallery, overrides earlier wrong URLs.
  2. Restored Altamira `name="precio"` price regex with flexible fallbacks.
  3. Added a phantom-pool guard to `groundFeatures()`: a pool counts only on an explicit phrase ("private/communal/swimming … pool", "pool area/deck/: yes"); otherwise strip pool from features AND sentence-scrub it from the description.

## Verification
- `node` fixture suite (regex compile + behaviour for all 4 paths) — PASS
- `npx supabase@2.40.7 functions deploy sophia-bot` — **SUCCESS, LIVE.** CLI 2.108.0's bundler is broken (`entrypoint path does not exist` on every function); pinning to `supabase@2.40.7` bundled + deployed cleanly (Deno type-check passes on upload).
- Post-deploy health: `curl …/functions/v1/sophia-bot/health` → **HTTP 200** (2.3s); root webhook → **HTTP 200**.

## Remaining Notes (deferred — need live-page HTML, route to a planned increment)
- **ALTIA** dup/missing photos — Vue SPA: raw-HTML fetch sees only a subset of lazy-loaded gallery; cloudfront size-variants defeat the URL-level Set dedup. Needs a full-gallery source + dedup by image id.
- **REMU** coords / Google-maps link — `parsePortalStructured` coord patterns don't match REMU's actual marker structure → no/ wrong pin.
- **Model:** bank extraction runs on Sonnet 4.6 (Opus 4.8 path reverted 2026-06-22 after an OpenRouter billing outage). Not changed here.
- Lauren's "image grabber" = the paste-link → portal-scraper gallery path; these fixes make it reliable for REMU + kill the Altamira phantom pool. Re-test REMU + Altamira after deploy.
