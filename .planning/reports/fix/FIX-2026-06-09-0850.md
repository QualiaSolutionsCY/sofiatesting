# Fix Report - 2026-06-09 08:50

**Symptom:** After the 08:04 extraction fix, Lauren reported location STILL wrong — but on the **Zyprus listing's location field**, not Sophia's WhatsApp reply.
**Mode:** general (root-cause via live evidence)
**Outcome:** fixed + verified against live taxonomy

## Feedback Loop
- Pulled the 3 real bank URLs Lauren tested at 08:06–08:27 (post-deploy) from `chat_history`.
- Reproduced each page with Firecrawl → Sophia's location STRINGS were all correct (Omonoia/Limassol, Mouttagiaka/Limassol, Agia Varvara/Nicosia).
- Queried the live Zyprus taxonomy via `/taxonomy-debug?q=` → found the area names don't exist as spelled.
- Final check: new `/taxonomy-debug?match=` endpoint runs the real `findLocationUuid` live.

## Root Cause
`supabase/functions/sophia-bot/zyprus/locations.ts` — the extraction was correct, but the Zyprus taxonomy uses different transliterations than the bank portals, and `findLocationUuid` only did substring/exact word matching:
- Bank "Omonoia" vs Zyprus node "**Omonia**, Limassol City Centre" (1-letter diff) → no match.
- Bank "Mouttagiaka" vs Zyprus node "**Moutagiaka**" (double-t vs single-t) → no match.
- When only the district word ("limassol") matched, EVERY "X, Limassol City Centre" node tied on score and the tie broke arbitrarily → "Agios Nektarios, Limassol City Centre" (a real node) won. That was the "Agios Nektarios default."

## Patch
- `zyprus/locations.ts`:
  - Added `normalizeLocWord` + `editDistance` + `fuzzyAreaEq` (accent-stripping Levenshtein, ~20% tolerance) and applied it to the first-word (specific area) match → "Omonoia"↔"Omonia", "Mouttagiaka"↔"Moutagiaka".
  - Track `specificAreaMatched`; when the best match matched ONLY the district word, fall back to the **generic district node** ("Limassol City Centre") instead of an arbitrary specific area, and log a warning for manual review.
  - District fallback node now prefers the most generic node (fewest words) over a specific-area node.
- `index.ts`: added read-only `/taxonomy-debug?match=<loc>` diagnostic that runs `findLocationUuid` live.

## Verification (live `?match=`)
- `Omonoia, Limassol`  → **Omonia, Limassol City Centre**  PASS
- `Mouttagiaka, Limassol` → **Moutagiaka**  PASS
- `Agia Varvara, Nicosia` → **Agia Varvara**  PASS
- `Neapoli, Limassol` (control) → Neapoli  PASS (unbroken)
- `Kathikas, Paphos` (control) → Kathikas  PASS (unbroken)

## Remaining Notes
- Listings created BEFORE this deploy (Zyprus 43229 Altia, 43230 Gordian) still have the wrong `field_location` — they need manual correction or re-upload. This fix only affects new uploads.
- Genuinely-missing areas (no close taxonomy node) now resolve to the correct **district** node (flagged), not a random area — the reviewer sets the exact area.
