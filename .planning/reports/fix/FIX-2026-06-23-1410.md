# Fix Report - 2026-06-23 14:10

**Symptom:** "Send email" button wasn't functional; need a working Delete in the actions dropdown.
**Mode:** feature (quick)
**Outcome:** fixed

## Root Cause
- Send email: `lib/invoices/integrations/manual-provider.ts:13` is a stub returning "manual-copy-ready" — the button queued a record but never sent. No real send path.
- Delete: only existed inside "cancel"; no dedicated Delete menu item.

## Patch
- `lib/invoices/actions/documents.ts` — new `sendInvoiceEmailAction(id,to)`: builds the PDF server-side and emails it via Resend (already used elsewhere in the app), attaches the unified-filename PDF, records the delivery.
- `components/invoices/redesign/App.tsx` — "Send email" now prompts for the recipient and calls `sendInvoiceEmailAction` (real send + toast on success/failure); added a `delete` case with a confirm → `deleteDocumentAction`.
- `components/invoices/redesign/ledger/DetailPane.tsx` — added a "Delete permanently" item to the More-actions dropdown.

## Verification
- `npx tsc --noEmit` - PASS (exit 0)
- eslint: only pre-existing warnings, none from these changes.

## Remaining Notes
- Recipient email is entered at send time (no real client email is stored — the UI address was a synthetic placeholder). `INVOICE_EMAIL_FROM` env overrides the from-address (defaults to the verified sophia@zyprus.com).
