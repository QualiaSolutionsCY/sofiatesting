# Fix Report - 2026-06-17 14:09

**Symptom:** Remove the Quantity column from the invoice template and make it render correctly everywhere; plus same-session follow-ups (Monthly/Yearly filter, clear test invoices, auto-approve status).
**Mode:** frontend
**Outcome:** fixed

## Feedback Loop
- Initial: inspect template surfaces (`grep -i qty` across TemplatePreview, pdf.ts, invoices.css)
- Final passing check: `node_modules/.bin/tsc --noEmit` = 0 errors; prod deploy READY; screenshot confirms table = Item · Description · Unit Price · Total

## Root Cause
`components/invoices/redesign/ledger/TemplatePreview.tsx:106` — the on-screen HTML template hardcoded a `<th>Qty</th>` + `<td>{l.qty}</td>` column. The generated A4 PDF (`lib/invoices/pdf.ts`) already had NO Qty column (it uses `#` · Description · Unit · Total), so the two surfaces disagreed.

## Patch
- `TemplatePreview.tsx` — drop Qty `<th>`/`<td>`; filler row 5→4 tds; source-row colSpan 5→4.
- `app/invoices/invoices.css` — `.template-table-ruled` nth-child widths re-mapped to 4 columns.
- (PDF needed no change — already Qty-less, so template now matches everywhere: detail preview, PDFLightbox, batch preview, PDF.)

### Same-session follow-ups (shipped, separate commits)
- Monthly/Yearly added to the status filter menu (Doc gains `recurrence`, mapped in adapter, filtered in ListPane).
- Cleared test invoices (James Spyrou ×2, Marios Andreou ×3) from the live DB.
- Admin auto-create now lands at status **"approved"** (new `markApprovedOnlyAction`, no auto-numbering) instead of numbered/unpaid; list row shows an "Approved" badge; row date formatted.

## Verification
- `node_modules/.bin/tsc --noEmit` — PASS (0 errors, each commit)
- Production deploy — PASS (READY, aliased to sofiatesting.vercel.app)
- Visual (owner screenshot) — PASS (Qty column gone)

## Remaining Notes
- "Sophia sending old invoice on WhatsApp": send path (`documents.ts:157`) regenerates the PDF fresh via `buildDocumentPdfBytes`, so newly created invoices carry all fixes; old messages already in chat are stale history.
- "Wrong amount in PDF": unconfirmed; create-path math is correct for 19%. `calculateVat` hardcodes 19% and ignores custom `vatRate` — candidate bug if a non-19% rate is used.
- Production runs the Friday branch (`deploy/invoices-friday`), not `main` — reconciliation pending.
