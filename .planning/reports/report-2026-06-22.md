# Session Report — 2026-06-22

**Project:** sofiatesting (Sophia – Zyprus)
**Employee:** Fawzi Goussous (OWNER)
**Branch:** main
**Phase:** 4 of 4 — Handoff (M9, setup)
**Report ID:** QS-REPORT-05
**Date:** 2026-06-22

## What Was Done

- **Emergency-restored the live SOPHIA WhatsApp bot.** Moayad's separately-deployed `sophia-bot` had taken her off the OpenRouter path (OpenRouter key showed "last used 2 days ago") / onto the free Nemotron model, so she was misbehaving for agents in production. Redeployed the `sophia-bot` edge function from clean git to revert the bad deploy. Why: production agents (Fawzi, Lauren, others) were getting wrong/no responses.
- **Set every SOPHIA model to `anthropic/claude-sonnet-4.6` via OpenRouter** — primary, pro, fallback (`ai-chat.ts`) + vision (`image-classifier.ts`) + webhook breadcrumb (commit `249eefc`). Deployed; confirmed live (OpenRouter key went from "2 days ago" → active, `chat_history` shows real replies flowing). Why: owner requirement — Sonnet 4.6 quality, no free-tier models.
- **Corrected `main`.** PR #29 (`feature/m9-phase2-docs`, another session) had reset the model constants back to free Nemotron; rebased the Sonnet fix on top and pushed so git matches the live function. Why: stop the next redeploy from regressing SOPHIA again (this free-model swap has now recurred twice).
- **Root-caused the "I'm taking longer than expected…" message Fawzi received.** It is a 110s tool-loop timeout (`ai-chat.ts:626-643`), not a crash — SOPHIA's tool-calling loop on the heavy Altia bank-portal upload exceeded the edge-function time budget and returned the graceful-degradation fallback. Switching the PRO/upload path to Sonnet 4.6 (slower per round) made the heaviest uploads more likely to tip over the budget. Why: explain the timeout and scope the fix.

_Context (earlier in this multi-day session):_ M9 Phase-1 invoice fixes (the 2026-06-17 Marios meeting) were built, verified — caught + fixed 3 HIGH money-path bugs — deployed to Vercel prod, then `main` was reconciled (our verified line won; Moayad's parallel invoice line discarded; redundant branches cleaned up).

## Blockers

- **Decision pending (owner):** whether to split the model — keep chat/vision on Sonnet 4.6 but move `PRO_MODEL` (uploads, portal extraction, email) to a faster model (`anthropic/claude-haiku-4.5`) so heavy bank-portal uploads stop hitting the 110s timeout. Recommended, but not yet applied.

## Next Steps

1. Decide on the `PRO_MODEL` split (Haiku 4.5 for the upload/portal path vs all-Sonnet) — if yes, set `PRO_MODEL = "anthropic/claude-haiku-4.5"`, redeploy `sophia-bot`, confirm a bank-portal upload completes in-budget.
2. Delete the stale `fix/*` branches that still carry the free-Nemotron model swap, so it can't be redeployed onto SOPHIA again.

## Commits (today, 2026-06-22)

```
249eefc fix(sophia): Claude Sonnet 4.6 via OpenRouter for every channel (chat/pro/vision)
```
_(Other 2026-06-21/22 commits on main — PR #29, security/QA sweep, ERP tracking bind — are from other sessions, not this shift.)_
