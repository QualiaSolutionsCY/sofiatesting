# Session Report — 2026-06-28

**Project:** sofiatesting (Sophia AI assistant + invoicing admin panel)
**Employee:** Moayad
**Branch:** main
**Phase:** 4 of 4 — Handoff (operate-mode hotfixes)
**Date:** 2026-06-28

## What Was Done
- **Fixed Sophia's "technical difficulties" outage.** Why: OpenRouter hit its **$700 spend cap** → transient 429s, and the fallback was the *same* `anthropic/claude-sonnet-4.6` so it couldn't escape. Added Retry-After-aware + jittered 429 backoff in `sophia-bot/services/ai-chat.ts` and redeployed the edge function (live).
- **Restored the invoicing panel's reachability.** Why: the office's **Orange ISP SNI-blocks all `*.vercel.app`** (proven: same Vercel IP, `vercel.com` SNI completes TLS, `sofiatesting.vercel.app` SNI resets; loads on mobile data). Attached the Vercel-managed custom domain **`invoices.qualiasolutions.net`** to the project — reachable past the filter, login verified (`trustHost`).
- **Fixed 4 invoice-panel bugs + shipped to main.** Why: monthly-run amount field had a stuck `0`; "Send email" sent a generic body instead of the composed "on behalf of Marios … via Sophia" message; the "Commission flagged" badge was unwanted; the new-invoice composer's unit-price had the same stuck-`0`.
- **Fixed the panel's stale-state-after-action bug.** Why: approve/cancel reconciled the UI *after* the best-effort group/Marios sends, so a notification throw (Wasender token) skipped the reconcile and left a stale row until manual Refresh. Reconcile now runs the instant the mutation persists, with a refetch fallback on error.
- **Ran a full live e2e of the invoice panel via Claude-in-Chrome.** Why: verify everything before handoff. PASS on wiring (all buttons/menus), filtering (Cancelled 7 / Draft 14 / Monthly 3 / All 45), PDF preview = downloaded PDF (byte-identical), and the full lifecycle create → approve (№ 11461) → send (default + edit-message override) → cancel → credit note (№ 10104). Operator confirmed the group post and both emails live.

## Blockers
- **OpenRouter $700 spend cap** is the real ceiling for Sophia — keep credit topped up / raise the limit (OWNER action, not EMPLOYEE).
- **Orange ISP blocks `*.vercel.app`** on the office network — the original `sofiatesting.vercel.app` stays unreachable there until the ISP/router whitelists it. Use `invoices.qualiasolutions.net`. (Internal bridge `sophia-bot → sofiatesting.vercel.app/api/sophia/intent` is unaffected — it runs on Supabase's network.)

## Next Steps
1. **Ship the OpenRouter 429 fix to `main`** — it's deployed live but stranded on `fix/openrouter-429-resilience` (cleaned up in this report's hygiene step).
2. (Left per operator) Fix the **Settings "WhatsApp accounting group: Wasender 401" false-negative** health check — the same notification throw the auto-refresh fix now tolerates.
3. (Optional, durable) Give Sophia a **cross-provider fallback** (Anthropic-direct) so an OpenRouter cap can't black her out.

## Commits (this session)
- `db3844f` fix(invoices): auto-refresh after approve/cancel + composer unit-price 0
- `bab1cea` fix(invoices): remove the "Commission flagged" badge from the invoice detail view
- `eadbc55` fix(invoices): email the message shown in the client-delivery box (not a generic body)
- `a915550` fix(invoices): monthly-run amount field — clear the stuck "0", allow decimals
- `ea9a3a1` fix(sophia): ride out OpenRouter 429s — honor Retry-After (capped) + jitter *(deployed; on branch fix/openrouter-429-resilience)*
